interface:
  display_name: "Guanlan Monitor Quality Gate"
  short_description: "Decide whether captured evidence can enter the V4 build"
  default_prompt: "Use $guanlan-monitor-quality-gate to evaluate one structured source-intake run without repeating collection or adding blockers."
policy:
  allow_implicit_invocation: true
