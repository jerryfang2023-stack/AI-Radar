interface:
  display_name: "Guanlan Daily Monitor"
  short_description: "Capture daily immutable source evidence for V4"
  default_prompt: "Use $guanlan-daily-monitor to run or repair one bounded source-capture attempt and route failures to their earliest owner."
policy:
  allow_implicit_invocation: true
