interface:
  display_name: "观澜每日监测 QC"
  short_description: "质检 V4 每日来源采集的全文、出处和结构化 intake 质量。"
  default_prompt: "Use $guanlan-daily-monitor-qc to audit one V4 source-intake run, identify provenance and full-text failures, check original-source coverage, keep weak material discovery-only, and output a Markdown QC report."
policy:
  allow_implicit_invocation: true
