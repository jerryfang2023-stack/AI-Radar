interface:
  display_name: "Guanlan Daily Monitor QC"
  short_description: "Audit V4 source-intake semantics on demand"
  default_prompt: "Use $guanlan-daily-monitor-qc to inspect suspicious source intake and route each finding to its earliest owner."
policy:
  allow_implicit_invocation: true
