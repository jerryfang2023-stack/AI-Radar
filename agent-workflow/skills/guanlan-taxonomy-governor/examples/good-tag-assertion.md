# Good TagAssertion

An accepted Claim explicitly says “released a multimodal model with open weights.” Separate `multimodal` and `open_weights` assertions reference that Claim and its exact source span.

The same Claim may also produce `product_form.model` as a FacetAssertion. It does not become a technical Tag, and the event type remains a separate CanonicalEvent field.
