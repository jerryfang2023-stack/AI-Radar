interface:
  display_name: "Guanlan Typography QC"
  short_description: "Audit V4 typography hierarchy and token discipline"
  default_prompt: "Use $guanlan-typography-qc to audit the requested WaveSight pages and return concrete token-mapped typography fixes."
policy:
  allow_implicit_invocation: true
