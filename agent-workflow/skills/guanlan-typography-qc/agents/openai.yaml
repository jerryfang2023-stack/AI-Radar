interface:
  display_name: "观澜字体质检"
  short_description: "Audit WaveSight page typography and hierarchy rules."
  default_prompt: "Use $guanlan-typography-qc to audit a WaveSight AI page for typography hierarchy and position-level font rules."
policy:
  allow_implicit_invocation: true
