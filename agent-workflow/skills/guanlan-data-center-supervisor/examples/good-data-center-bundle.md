# Good Data Center Bundle

A canonical event references an exact Claim span and SourceArtifact. FDE, hardware, and tags reference the same accepted evidence. Missing values remain empty and appear in `missing_fields` or `undisclosed_fields`.
