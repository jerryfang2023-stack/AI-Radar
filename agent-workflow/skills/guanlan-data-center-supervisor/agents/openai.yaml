interface:
  display_name: "Guanlan Data Center Supervisor"
  short_description: "Run and repair the source-traceable V4 factual chain"
  default_prompt: "Use $guanlan-data-center-supervisor to run or repair the requested Data Center V4 chain and verify its lineage gates."
policy:
  allow_implicit_invocation: true
