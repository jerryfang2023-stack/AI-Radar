interface:
  display_name: "Guanlan Raw Pool Card"
  short_description: "Run or audit the explicit WaveSight V3 Raw / Pool / Card compatibility flow."
  default_prompt: "Use $guanlan-raw-pool-card only for an explicit V3 compatibility task: run or repair Raw, Pool, and the three allowed Signal Card types without treating Card outputs as Data Center V4 canonical data."
policy:
  allow_implicit_invocation: false
