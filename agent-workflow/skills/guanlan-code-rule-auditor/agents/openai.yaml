interface:
  display_name: "Guanlan Code Rule Auditor"
  short_description: "Audit WaveSight code, rules, workflows, and gates"
  default_prompt: "Audit the active WaveSight execution chain and report evidence-backed defects without changing files."
policy:
  allow_implicit_invocation: true