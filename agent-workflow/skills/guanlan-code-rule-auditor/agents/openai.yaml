interface:
  display_name: "Guanlan Code Rule Auditor"
  short_description: "Audit active code rules workflows and gates"
  default_prompt: "Use $guanlan-code-rule-auditor to trace the active WaveSight chain and report evidence-backed findings without mutation."
policy:
  allow_implicit_invocation: true
