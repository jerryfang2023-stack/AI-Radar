interface:
  display_name: "Guanlan Code Rule Auditor"
  short_description: "Read-only audit of active rules and execution paths"
  default_prompt: "Use $guanlan-code-rule-auditor to perform a read-only audit of the active WaveSight chain and report evidence-backed findings; route requested Skill repairs to $guanlan-skill-editor."
policy:
  allow_implicit_invocation: true
