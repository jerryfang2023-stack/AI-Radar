interface:
  display_name: "Guanlan Skill Editor"
  short_description: "Create or repair governed WaveSight Skills"
  default_prompt: "Use $guanlan-skill-editor when the user requests creating or repairing Guanlan Skills; use $guanlan-code-rule-auditor for audit-only defect discovery."
policy:
  allow_implicit_invocation: true
