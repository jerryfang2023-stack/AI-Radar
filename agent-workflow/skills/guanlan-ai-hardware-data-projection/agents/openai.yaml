interface:
  display_name: "Guanlan AI Hardware Projection"
  short_description: "Build Claim-native hardware facts and snapshots"
  default_prompt: "Use $guanlan-ai-hardware-data-projection to build evidence-linked hardware facts, snapshots, differences, and eligible records."
policy:
  allow_implicit_invocation: true
