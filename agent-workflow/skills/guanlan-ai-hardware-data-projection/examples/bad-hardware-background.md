# Bad Hardware Background

An Apple/OpenAI lawsuit mentions servers in background paragraphs and is emitted as hardware. It fails because the normalized event is litigation, not hardware product, capacity, supply, or deployment.
