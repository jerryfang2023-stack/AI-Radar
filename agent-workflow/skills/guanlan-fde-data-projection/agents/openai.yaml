interface:
  display_name: "Guanlan FDE Data Projection"
  short_description: "Build Claim-native implementation observations"
  default_prompt: "Use $guanlan-fde-data-projection to build evidence-linked FDE observations, dossiers, and eligible publication records."
policy:
  allow_implicit_invocation: true
