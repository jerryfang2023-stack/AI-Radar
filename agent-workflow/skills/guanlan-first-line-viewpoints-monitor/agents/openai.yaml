interface:
  display_name: "Guanlan First-Line Viewpoints Monitor"
  short_description: "Supervise current and historical First-Line Viewpoints data and the V4 projection."
  default_prompt: "Supervise FLV-V1.1.0-history-backfill for the active date: validate current builders data, committed history provenance, and the merged V4 projection; keep afternoon intake independent, treat Guanlan Vault timelines as a downstream local projection, and record prevention for recurring failures."
policy:
  allow_implicit_invocation: false
