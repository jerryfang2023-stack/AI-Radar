interface:
  display_name: "Follow Builders"
  short_description: "Curate source-linked AI builder viewpoints"
  default_prompt: "Use $follow-builders to curate source-linked builder commentary for the WaveSight First-Line Viewpoints lane."
policy:
  allow_implicit_invocation: true
