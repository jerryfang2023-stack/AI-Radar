# Good RawDocument

The record preserves original and cleaned text, source and snapshot references, capture diagnostics and hashes. It contains no score, meaning, recommendation, or downstream-use field.
