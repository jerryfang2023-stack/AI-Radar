# Quality Gates Report

生成时间：2026/5/10 13:02:38

## 结论

- 模式：v2content
- 日期参数：2026-05-10
- 状态：passed
- 检查项：2
- 失败项：0

## 检查明细

### 1. v2-content-gate syntax

- 命令：`node --check agent-workflow/tools/v2-content-gate.mjs`
- 状态：passed (0)
- stdout：skipped: child_process spawn blocked (EPERM) in this environment
- stderr：-

### 2. run v2 content gate

- 命令：`node agent-workflow/tools/v2-content-gate.mjs --date=2026-05-10`
- 状态：passed (0)
- stdout：| Point calibration includes source, original view, interpretation, usage and relations | passed | 3 point items meet relation gate | / | V2 site root exists | passed | 01-SiteV2/site | / ## 说明 / - 本闸门检查当前 V2 内容生产路径 `01-SiteV2/content/`。 / - 它不运行旧 `04-Site` 同步，也不部署 Netlify。 / - 旧 `04-Site` 已归档，本检查只确认 V2 生产内容链路是否具备可入站基础。
- stderr：-


## 说明

- 本脚本是 `quality-gates.md` 的统一入口。
- V2-only 阶段默认检查 `01-SiteV2/site/` 与 `agent-workflow/tools/v2-*.mjs`。
- 旧 `04-Site` 已归档，不再作为 `content`、`site` 或 `all` 模式的默认目标。
- `content`、`point` 和 `all` 会运行 V2 content gate；需要指定日期时使用 `--date=YYYY-MM-DD`。
- `automation` 默认不真实运行统一同步闸门；需要显式传 `--run-sync-gate`。
- 未覆盖的浏览器截图、多身份权限和人工内容判断，仍需 QA Agent 单独验收。
