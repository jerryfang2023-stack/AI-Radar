# Quality Gates Report

生成时间：2026/5/4 20:56:10

## 结论

- 模式：automation
- 日期参数：2026-05-04
- 状态：passed
- 检查项：2
- 失败项：0

## 检查明细

### 1. unified-site-sync syntax

- 命令：`node --check agent-workflow/tools/unified-site-sync.mjs`
- 状态：passed (0)
- stdout：-
- stderr：-

### 2. run unified site sync gate

- 命令：`node agent-workflow/tools/unified-site-sync.mjs --date=2026-05-04`
- 状态：passed (0)
- stdout：## 处理规则 / - 只有 AI商业雷达、AI机会评分、The Point 三类当天 Markdown 同时就绪，才允许同步网站。 / - 任一内容缺失、空跑、失败、待补充或字段不完整，均阻止入站。 / - 同步前先备份网站数据文件；同步后关系检查或 The Point 质量检查出现硬错误，则恢复备份。 / - Priority Engine 2.0 的 Judgment Node 覆盖率是 ai-3 硬闸门：Priority Row 必须 100% 进入 Judgment Node，Judgment Node 不得断链，priorityEngine 摘要必须与实际数据一致。 / - 内容生产任务不得直接运行网站同步，统一由本闸门执行。
- stderr：-


## 说明

- 本脚本是 `quality-gates.md` 的统一入口。
- `content` 和 `all` 会运行 `sync-data.mjs`，可能更新 `04-Site/data/radar-data.json` 与 `radar-data.js`。
- `automation` 默认不真实运行统一同步闸门；需要显式传 `--run-sync-gate`。
- 未覆盖的浏览器截图、多身份权限和人工内容判断，仍需 QA Agent 单独验收。
