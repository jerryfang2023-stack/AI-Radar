{
  "name": "guanlan-funding-miniprogram",
  "version": "0.7.2",
  "private": true,
  "scripts": {
    "build:funding": "node scripts/build-funding-data.mjs",
    "build:reports": "node scripts/build-report-data.mjs",
    "build:data": "npm run build:funding && npm run build:reports",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-project.mjs",
    "verify": "npm run build:data && npm test && npm run validate"
  }
}
