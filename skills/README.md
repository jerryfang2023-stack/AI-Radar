# Guanlan Skill Router

This directory no longer contains executable Skill copies.

- Project rule source: `agent-workflow/skills/`
- Installed runtime mirror: `C:/Users/86186/.skill-store/`
- Registry: `agent-workflow/skills/skill-registry.md`

Historical reports may mention paths below `skills/`; they are evidence of earlier runs, not current execution truth. Do not restore duplicate `SKILL.md` files here.
